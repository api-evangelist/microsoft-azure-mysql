naftiko: 1.0.0-alpha2
info:
  label: Azure Database for MySQL REST API — Operations
  description: 'Azure Database for MySQL REST API — Operations. 1 operations. Lead operation: Azure Database for MySQL REST
    API List operations. Self-contained Naftiko capability covering one Microsoft Azure Mysql business surface.'
  tags:
  - Microsoft Azure Mysql
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_MYSQL_API_KEY: MICROSOFT_AZURE_MYSQL_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-mysql-operations
    baseUri: https://management.azure.com
    description: Azure Database for MySQL REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.DBforMySQL-operations
      path: /providers/Microsoft.DBforMySQL/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure Database for MySQL REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_MYSQL_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-mysql-operations-rest
    port: 8080
    description: REST adapter for Azure Database for MySQL REST API — Operations. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-dbformysql/operations
      name: providers-microsoft-dbformysql-operations
      description: REST surface for providers-Microsoft.DBforMySQL-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure Database for MySQL REST API List operations
        call: microsoft-azure-mysql-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-mysql-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Database for MySQL REST API — Operations. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: azure-database-mysql-rest-api
      description: Azure Database for MySQL REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-mysql-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
